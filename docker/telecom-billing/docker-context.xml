<?xml version="1.0" encoding="UTF-8"?>
<!--
  Tomcat context.xml for telecom-billing (telnova)
  DB credentials are read from environment variables injected by Docker Compose.
  This file is copied into /usr/local/tomcat/conf/context.xml at image build time.
-->
<Context>

    <!-- JNDI DataSource – PostgreSQL -->
    <Resource
        name="jdbc/TelecomDB"
        auth="Container"
        type="javax.sql.DataSource"
        driverClassName="org.postgresql.Driver"
        url="jdbc:postgresql://${DB_HOST}:${DB_PORT}/${DB_NAME}"
        username="${DB_USER}"
        password="${DB_PASSWORD}"
        maxTotal="20"
        maxIdle="10"
        maxWaitMillis="10000"
        validationQuery="SELECT 1"
        testOnBorrow="true"
    />

</Context>
